---
name: wrath-status
description: Show Wrath runtime status. Use for /wrath-status or when asked if Wrath is working.
---

# Wrath status

Call MCP **`wrath_status`** (fallback `wrath_config` / `wrath_doctor`).

Report in ≤6 lines: version, ON/OFF, profile, strict/orch/il/privacy, budget, config path, recent counts.
