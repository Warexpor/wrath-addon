YOLO_BODY = """YOLO profile ON.
Soft guards: force-push / reset --hard / clean -fdx / pipe-to-shell / bulk upload allowed.
Still blocked: root/home wipe, mkfs/dd, fork bombs, .git/ writes, secrets→paste, project deny.
Confirm blast radius yourself. Off: /wrath-profile default · /wrath-yolo-off · WRATH_YOLO=0
"""
